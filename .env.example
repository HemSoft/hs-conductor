# hs-conductor Environment Configuration
# Copy to .env (setup.ps1 does this automatically)

# ============================================================
# INNGEST CONFIGURATION
# ============================================================
# For local development: these default values work fine (no real keys needed)
# For production (Inngest Cloud): generate real keys with: openssl rand -hex 32
INNGEST_EVENT_KEY=test-event-key-local-development
INNGEST_SIGNING_KEY=test-signing-key-local-development

# Inngest dev server URL (auto-started by run.ps1)
INNGEST_BASE_URL=http://localhost:2901

# Local development mode (1 = use local inngest-cli dev server)
INNGEST_DEV=1

# ============================================================
# GITHUB COPILOT SDK
# ============================================================
# Uses GitHub Copilot CLI for authentication
# Install: npm install -g @github/copilot-cli
# Authenticate: copilot auth

# Default model for AI Worker (optional)
# Options: claude-sonnet-4.5, gpt-5, etc.
COPILOT_MODEL=claude-sonnet-4.5

# ============================================================
# APPLICATION SETTINGS
# ============================================================
# Data directory for plans, templates, and runs
CONDUCTOR_DATA_PATH=./data

